name: Keep Render Instance Warm

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the API
        run: curl -sf https://library-rest-api-j3ri.onrender.com/api/v1/health
